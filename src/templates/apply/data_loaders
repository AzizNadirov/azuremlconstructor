# Loads data inputs to the program
import argparse
import pandas as pd
{# comment input_name: [filename, method_name] #}

parser = argparse.ArgumentParser()

{% for k, v in inputs.items() %}
parser.add.argument('--{{k}}', type=str)
{% endfor %}

args = parser.parse_args()

{% for k, v in inputs.items() %}
{% if v[1]==-1 %}
{{k}} = args.{{k}}
{% else %}
{{k}} = pd.{{v[1]}}(args.{{k}} + "/{{v[0]}}")
{% endif %}
{% endfor %}
